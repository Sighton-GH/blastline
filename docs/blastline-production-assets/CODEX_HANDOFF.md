# Codex handoff

Use the production assets plus the visual targets under `docs/art-reference/` to converge the real running BLASTLINE game on the references.

Non-negotiable validation loop:

1. run the real game;
2. capture a real browser screenshot at 390×844 DPR 1;
3. compare to the relevant reference image;
4. identify the 1–3 largest perceptual mismatches;
5. implement targeted fixes;
6. rerun and capture the exact same state;
7. compare again;
8. keep/revise/revert based on evidence;
9. repeat until obvious mismatches are gone.

Also validate 1365×768 DPR 1.

Never fabricate a screenshot of the current site. The bridge road must remain flat/straight/planar and be constructed mathematically; only suspension cables curve.
