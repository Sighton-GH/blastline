# BLASTLINE Codex package

For Codex, use the 10 visual reference images in `docs/art-reference/` plus the production-asset handoff documents in this folder.

The generated Batch 1–10 master assets are user-approved and available under `assets/source/blastline/`. Derive optimized runtime assets under `assets/blastline/` with `tools/extract-production-assets.py`, and keep the original references separate.
