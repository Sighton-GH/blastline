# BLASTLINE Codex package

For Codex, use the 10 visual reference images in `docs/art-reference/` plus the production-asset handoff documents in this folder.

The generated Batch 1–10 master assets are user-approved. When their binary image files are available to the coding environment, place untouched source masters under `assets/source/blastline/`, derive optimized runtime assets under `assets/blastline/`, and keep the original references separate.
