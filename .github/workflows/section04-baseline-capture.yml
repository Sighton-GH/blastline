name: Section 4 reference staging

on:
  push:
    branches: [section-04-enemies-combat]

jobs:
  stage-references:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: section04-reference-library
          path: docs/art-reference
